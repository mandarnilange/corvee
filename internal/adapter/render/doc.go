// Package render produces the static HTML dashboard (board, tree,
// summary views). Templates and themes are embedded via embed.FS so
// the binary has no runtime asset dependencies.
package render
